module tb_check_win_condition_wrapper;
  reg [3:0] recent_x, recent_y;
  reg [1:0] piece_type;
  wire win;

  check_win_condition_wrapper #(10) UUT (.*);

  initial begin
    // Set piece_type, recent_x, and recent_y
    // Call $display or $monitor to print the output 'win'
    $finish;
  end
endmodule
